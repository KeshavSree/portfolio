<!-- Project detail overlay — click-to-expand from wave cards -->
<div id="project-overlay" class="project-overlay">
	<div class="overlay-backdrop"></div>
	<div class="overlay-panel">
		<button id="overlay-close" class="overlay-close" aria-label="Close">&times;</button>
		<img id="overlay-img" src="" alt="" class="overlay-img">
		<div class="overlay-nav">
			<button id="overlay-prev" class="overlay-nav-btn" aria-label="Previous image">&#8249;</button>
			<span id="overlay-counter" class="overlay-counter"></span>
			<button id="overlay-next" class="overlay-nav-btn" aria-label="Next image">&#8250;</button>
		</div>
		<h2 id="overlay-title" class="overlay-title"></h2>
		<p id="overlay-desc" class="overlay-desc"></p>
		<div id="overlay-links" class="overlay-links"></div>
	</div>
</div>

<style is:global>
	.project-overlay {
		position: fixed;
		inset: 0;
		z-index: 20;
		display: flex;
		align-items: center;
		justify-content: center;
		opacity: 0;
		pointer-events: none;
		transition: opacity 0.35s ease;
	}

	.project-overlay.active {
		opacity: 1;
		pointer-events: auto;
	}

	.overlay-backdrop {
		position: absolute;
		inset: 0;
		background: rgba(0, 0, 0, 0.6);
		backdrop-filter: blur(8px);
		-webkit-backdrop-filter: blur(8px);
	}

	.overlay-panel {
		position: relative;
		z-index: 1;
		max-width: 1200px;
		width: 96%;
		max-height: 95vh;
		overflow-y: auto;
		background: rgba(255, 255, 255, 0.07);
		border: 1px solid rgba(255, 255, 255, 0.15);
		border-radius: 0.75rem;
		padding: 2rem;
	}

	.overlay-close {
		position: absolute;
		top: 0.75rem;
		right: 0.75rem;
		background: none;
		border: 1px solid rgba(255, 255, 255, 0.15);
		border-radius: 0.375rem;
		color: rgba(255, 255, 255, 0.5);
		font-size: 1.5rem;
		line-height: 1;
		width: 2rem;
		height: 2rem;
		cursor: pointer;
		display: flex;
		align-items: center;
		justify-content: center;
		transition: none;
	}

	.overlay-close:hover {
		color: #fff;
		border-color: rgba(255, 255, 255, 0.4);
		box-shadow: 0 0 8px 2px rgba(255, 255, 255, 0.6), 0 0 16px 4px rgba(255, 255, 255, 0.3);
	}

	.overlay-img {
		display: block;
		width: 100%;
		height: 72vh;
		object-fit: contain;
		margin: 0 auto;
		border-radius: 0.5rem;
		border: 1px solid rgba(255, 255, 255, 0.08);
		background: rgba(0, 0, 0, 0.2);
	}

	.overlay-nav {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 1rem;
		margin: 0.75rem 0 1.25rem;
	}

	.overlay-nav-btn {
		background: none;
		border: 1px solid rgba(255, 255, 255, 0.15);
		border-radius: 0.375rem;
		color: rgba(255, 255, 255, 0.5);
		font-size: 1.75rem;
		line-height: 1;
		width: 2.25rem;
		height: 2.25rem;
		cursor: pointer;
		display: flex;
		align-items: center;
		justify-content: center;
		transition: none;
	}

	.overlay-nav-btn:hover {
		color: #fff;
		border-color: rgba(255, 255, 255, 0.4);
		box-shadow: 0 0 8px 2px rgba(255, 255, 255, 0.6), 0 0 16px 4px rgba(255, 255, 255, 0.3);
	}

	.overlay-counter {
		font-family: monospace;
		font-size: 0.85rem;
		color: rgba(255, 255, 255, 0.5);
		min-width: 3rem;
		text-align: center;
	}

	.overlay-title {
		font-size: 1.5rem;
		font-weight: 600;
		margin: 0 0 0.5rem;
		opacity: 0.9;
	}

	.overlay-desc {
		font-size: 1rem;
		line-height: 1.7;
		opacity: 0.7;
		margin: 0 0 1.25rem;
	}

	.overlay-links {
		display: flex;
		gap: 0.75rem;
	}

	.overlay-link {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		font-family: monospace;
		font-size: 1rem;
		color: #fff;
		text-decoration: none;
		padding: 0.55rem 1.1rem;
		border: 1px solid rgba(255, 255, 255, 0.3);
		border-radius: 0.375rem;
		transition: none;
	}

	.overlay-link svg {
		width: 1.1rem;
		height: 1.1rem;
		fill: currentColor;
		flex-shrink: 0;
	}

	.overlay-link:hover {
		border-color: rgba(255, 255, 255, 0.6);
		box-shadow: 0 0 8px 2px rgba(255, 255, 255, 0.6), 0 0 16px 4px rgba(255, 255, 255, 0.3);
	}
</style>
